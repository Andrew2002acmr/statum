# IMPLEMENTATION-PLAN

## 2026-09-13 — Benefits: визуализация проектного контроля

- Пользовательский файл `benefits-project-control.png` добавлен в `src/assets/images/generated/benefits/` и подключён через `src/data/assets.ts`; существующий Astro image pipeline создаёт адаптивные варианты. `[Бриф]` `[Материалы]`
- `Benefits.astro` переведён на полноформатную медиакомпозицию с доступным альтернативным текстом, видимым disclosure-label и адаптивным положением контента. Данные пяти преимуществ и порядок секций не менялись. `[Рекомендация]`
- Для проверки предусмотрены format, Astro check, production build и Playwright smoke на 1440, 1024, 768 и 390 px. `[Рекомендация]`

## 2026-09-09 — блок процесса переработан

- Подготовлены production WebP: панорама 2880 px и мобильный кроп 1200 × 900 из утверждённой генерации Nano Banana Pro. `[Бриф]` `[Материалы]` `[Рекомендация]`
- `Process.astro` переведён на полноширинную медиакомпозицию: desktop-overlay для заголовка, последовательная mobile-композиция, семантический список из пяти шагов и видимая маркировка визуализации. `[Рекомендация]`
- По обратной связи пользователя вертикальный ритм уплотнён: outer padding уменьшен до 24/32/40 px, desktop hero ограничен диапазоном 432–512 px. `[Бриф]` `[Рекомендация]`
- Ассеты подключены через `src/data/assets.ts` и существующий Astro responsive image pipeline; итоговая задача включает build, check, lint, format и Playwright QA на контрольных ширинах. `[Материалы]` `[Рекомендация]`

## 2026-09-09 — интеграция выбранных визуализаций завершена

- `design/Hero.png` подключён в Hero через `src/data/assets.ts`; Astro формирует responsive AVIF/WebP, сохраняются eager loading и высокий приоритет LCP. Подготовлен отдельный мобильный кроп 1200 × 900. `[Бриф]` `[Материалы]` `[Рекомендация]`
- `about-real-renovation.webp` подключён в секцию About вместо душевой фотографии с lazy loading и фиксированным соотношением 4:3. `[Бриф]` `[Материалы]` `[Рекомендация]`
- Для обоих изображений добавлена видимая маркировка «Интерьерная визуализация». Следующий шаг — responsive QA и production build в рамках этой задачи. `[Рекомендация]`

## 2026-09-08 — пересмотр концепции, premium pilot

Пилот выполнен: два рекламных направления, две отклонённые попытки ретуши одного санузла, ровно 5/5 вызовов. Пользователь предпочёл первую версию A — hero-a-graphite-living-v1.png. Выбор зафиксирован; дальнейшая обработка и интеграция ожидают следующей команды. Интеграция и новая мобильная композиция сейчас не выполняются; прежние рекомендации «генерация не нужна» не ограничивают новый бриф. [Бриф] [Материалы]

Результаты, происхождение и проверка: [premium-pilot/README.md](../design/premium-pilot/README.md).

## 2026-09-08 — интеграция изображений завершена

- Подключены `src/data/assets.ts` и `src/components/ui/ResponsiveImage.astro`; секции Hero, About, Process, Services, Benefits, Projects и FinalCta используют подготовленные файлы по [манифесту](../design/image-manifest.md). `[Бриф]` `[Материалы]`
- Визуальная сетка сохранена в тёмно-графитовом и золотом направлении референса: Hero имеет градиент под текст, портфолио — пять равных 3:4 карточек, на mobile используются отдельные 4:3/16:7 кадры или grid из одной колонки. `[Макет]` `[Материалы]` `[Рекомендация]`
- В следующей контентной итерации нужны документальные кадры команды, замера, черновых работ и приёмки; до их появления не возвращать визуально неподтверждённые сервисные карточки. `[Материалы]` `[Рекомендация]`

## 2026-09-08 — подготовка изображений завершена; интеграция ожидает команды

- Подготовлены 32 WebP в `src/assets/images/prepared/` из 10 выбранных фотографий. [Манифест](../design/image-manifest.md) содержит исходники, изменения, пропорции, зоны текста и результаты проверки. `[Материалы]`
- Обычной фотокоррекции оказалось достаточно. Оригиналы, код, текущие импорты и зависимости сохранены. Созданы сравнительные листы в `design/image-review/`. `[Бриф]` `[Материалы]`
- Следующая отдельная задача: подключить реальные ассеты, учесть карточки 3:4 и мобильный Hero 4:3, проверить фактические подписи и адаптивность. Полные версии загружать по запросу; декоративные CTA-файлы не использовать как портфолио. На текущем этапе остановка до следующей команды. `[Бриф]` `[Рекомендация]`

## 2026-09-08 — завершён аудит; обработка и реализация ожидают команды

- Выполнен [визуальный аудит и подбор изображений](../design/image-audit.md): инвентаризация, четыре группы, рецепты обработки, карта блоков и недостающая съёмка. Страница проверена в Playwright на 1440/1024/768/390 и дополнительно 320 px. `[Материалы]`
- После отдельной команды: обычная коррекция `IMG_20220801_164013.jpg`, `IMG_20220321_161406.jpg`, `IMG_20220801_154610.jpg`, `IMG_20210924_173446.jpg`; минимальная подготовка деталей 005848, 161443, 161419 и 154642 из соответствующих серий. Точные пути и ограничения приведены в отчёте. `[Рекомендация]`
- Подтвердить группировку фотографий по объектам и подписи, затем подключать ассеты через Astro Picture/Image. Большинство исходников вертикальные; потребуется адаптация галереи, отдельный мобильный кроп Hero и корректный размер ресурса при cover. `[Рекомендация]` `[Предположение/TBD]`
- В отдельной задаче реализации исправить overflow Statistics на 320 px, композицию Hero, контраст кнопок, повторение блоков доверия и служебный контент. Недостающие фото мастера, замера и черновых работ получать съёмкой. `[Рекомендация]`
- Генеративная обработка для основного набора не требуется. Два условных резервных кадра и ограничения GPT‑Image‑2 описаны в отчёте. Код и фотографии на текущем этапе не изменялись; дальнейшая работа остановлена до команды пользователя. `[Бриф]` `[Рекомендация]`

## Легенда источников

- `[Макет]` - непосредственно видно в `design/statum-design.jpg`.
- `[Материалы]` - найдено среди существующих файлов проекта.
- `[Рекомендация]` - техническая, UX или дизайн-рекомендация.
- `[Предположение/TBD]` - предположение, требуется согласование с клиентом.
- `[Бриф]` - указано в исходной задаче.

## Зафиксированный стек

- Источник истины по стеку: [TECH-STACK.md](TECH-STACK.md). `[Рекомендация]`
- Основа реализации: Astro, TypeScript strict, Astro Components, обычный TypeScript для простой интерактивности. `[Рекомендация]`
- Стили: Tailwind CSS, CSS custom properties для дизайн-токенов, scoped CSS только для сложных отдельных секций. `[Рекомендация]`
- Форма: Astro Actions, Zod, состояния `loading`/`success`/`error`, honeypot, возможность Cloudflare Turnstile, доставка заявки `TBD`. `[Рекомендация]` `[Предположение/TBD]`
- Проверки: `astro check`, ESLint, Prettier, Playwright, viewport 1440/1024/768/390 px. `[Рекомендация]`

## Этап 0 - Техническая подготовка проекта

Задачи:

- Создать `docs/IMPLEMENTATION-MAP.md` и `docs/ASSET-MANIFEST.md`. `[Рекомендация]`
- Инициализировать Astro-проект в корне репозитория. `[Бриф]`
- Включить TypeScript strict, Tailwind CSS, CSS custom properties, ESLint, Prettier и Playwright. `[Рекомендация]`
- Настроить Astro Node adapter для будущих Astro Actions. `[Рекомендация]`
- Создать нейтральную техническую главную страницу без реализации секций лендинга. `[Бриф]`
- Проверить базовые команды качества и Playwright smoke-тест. `[Рекомендация]`

Результат:

- Пустой, но собираемый Astro-проект с утвержденными инструментами качества, практической картой реализации и реестром ассетов.

Проверка:

- Dev-сервер запускается.
- Production build проходит.
- `npm run check`, `npm run lint`, `npm run format:check` и `npm run test:e2e` выполняются.
- Playwright открывает базовую страницу и проверяет отсутствие горизонтального overflow.

Зависимости:

- Текущая документация.
- [TECH-STACK.md](TECH-STACK.md).

## Этап 1 - Контентные решения и базовая визуальная система

Задачи:

- Закрыть критичные вопросы из `docs/OPEN-QUESTIONS.md` для hero, формы и footer. `[Рекомендация]`
- Уточнить позиционирование: частный мастер, бригада, команда или компания. `[Бриф]` `[Макет]` `[Предположение/TBD]`
- Подтвердить услуги, регион, контакты, юридические данные, цифры и канал заявок. `[Предположение/TBD]`
- Решить, какие изображения можно использовать в production, а какие остаются локальными заглушками. `[Материалы]` `[Предположение/TBD]`
- Доработать дизайн-токены и базовые UI-компоненты под будущие секции. `[Рекомендация]`

Результат:

- Подтверждены критичные решения для первого экрана и формы, а базовая визуальная система готова к реализации секций.

Проверка:

- Для формы, контактов, услуг, юридического согласия и ключевых claims критичные `TBD` либо закрыты, либо явно скрыты из production UI.
- UI-компоненты не зависят от неподтверждённых данных.

Зависимости:

- Этап 0.

## Этап 2 - Дизайн-токены, базовые стили и ассеты

Задачи:

- Завести CSS custom properties для цветов, контейнера, отступов, радиусов, теней и других токенов. `[Рекомендация]`
- Связать Tailwind-конфигурацию с дизайн-токенами, чтобы не размножать произвольные значения. `[Рекомендация]`
- Подготовить локальные заглушки изображений с правильными пропорциями. `[Рекомендация]`
- Сохранить логотип и уникальную графику как отдельные SVG после получения исходников. `[Предположение/TBD]`
- Подключить Lucide как единственный набор иконок на этапе реализации. `[Рекомендация]`
- Исключить текущие загрязненные SVG с embedded base64 PNG из production-пути. `[Материалы]` `[Рекомендация]`
- После появления оригиналов использовать Astro Image/Picture и явно указывать размеры изображений. `[Рекомендация]`

Результат:

- Единая визуальная основа и безопасный слой временных ассетов, которые можно заменить без перестройки компонентов.

Проверка:

- Нет production-зависимости от конкретных размеров временных изображений.
- Изображения не вызывают layout shift.
- Используется один стиль иконок.

Зависимости:

- Этап 0.
- Этап 1 для подтверждённых материалов.

## Этап 3 - Базовый layout, header и hero

Задачи:

- Собрать семантический каркас страницы: `header`, `main`, `footer`. `[Рекомендация]`
- Реализовать header и hero как Astro Components. `[Рекомендация]`
- Реализовать мобильное меню на обычном TypeScript без React/Preact/Vue. `[Рекомендация]`
- Реализовать hero с оффером, CTA, фоном и контейнером формы. `[Макет]`
- Реализовать кнопки через Tailwind utilities и дизайн-токены. `[Макет]` `[Рекомендация]`

Результат:

- Первый экран сообщает оффер и дает путь к заявке на desktop и mobile.

Проверка:

- Проверить viewport 1440, 1024, 768 и 390 px.
- Убедиться, что нет горизонтального скролла и наложения элементов.
- Проверить клавиатурный доступ к навигации, CTA и форме.

Зависимости:

- Этап 2.

## Этап 4 - Контентные секции

Задачи:

- Реализовать секции как Astro Components: преимущества, позиционирование, процесс, услуги, "почему выбирают", работы, CTA, отзывы, футер. `[Макет]` `[Рекомендация]`
- Для сложных секций использовать scoped CSS только там, где Tailwind и токены становятся менее читаемыми. `[Рекомендация]`
- Для простого слайдера отзывов или работ предпочесть CSS Scroll Snap и TypeScript. `[Рекомендация]`
- Библиотеку слайдера не подключать без функциональной необходимости. `[Рекомендация]`
- Блок цифр показывать только после подтверждения значений. `[Макет]` `[Предположение/TBD]`
- Отзывы показывать только при наличии реального контента. `[Макет]` `[Предположение/TBD]`

Результат:

- Полная структура лендинга с утвержденным контентом или безопасными `TBD` в данных, не выводимыми как факты.

Проверка:

- У каждой секции есть понятная роль.
- На контрольных viewport нет горизонтального скролла.
- Неподтвержденные claims не показаны как реальные.

Зависимости:

- Этап 3.
- Этап 0 для фактического контента.

## Этап 5 - Форма и обработка заявки

Задачи:

- Реализовать форму через Astro Actions. `[Рекомендация]`
- Описать серверную схему валидации через Zod. `[Рекомендация]`
- Реализовать состояния `loading`, `success` и `error`. `[Рекомендация]`
- Добавить honeypot и предусмотреть место для Cloudflare Turnstile. `[Рекомендация]`
- Канал доставки заявки оставить `TBD` до выбора: email, Telegram или CRM. `[Предположение/TBD]`
- До подключения реального обработчика не показывать пользователю ложный success. `[Рекомендация]`
- Не добавлять NestJS, Express, базы данных, Redis или отдельное backend-приложение без согласования. `[Рекомендация]`

Результат:

- Форма доступна, валидируется и готова к подключению реальной доставки заявки.

Проверка:

- Пустые обязательные поля показывают ошибки.
- Невалидные данные блокируются Zod-схемой.
- Ошибка отправки отображается без потери данных.
- Форма проходит keyboard-only сценарий.
- До реального обработчика success не имитируется.

Зависимости:

- Этап 0: канал заявок и юридический текст.
- Этап 3: UI формы.

## Этап 6 - SEO, доступность, производительность

Задачи:

- Добавить title, description, Open Graph, canonical, structured data при наличии реальных данных. `[Рекомендация]`
- Проверить heading hierarchy и semantic landmarks. `[Рекомендация]`
- Проверить focus states, labels, alt text, contrast. `[Рекомендация]`
- Настроить оптимальную загрузку изображений через Astro Image/Picture после появления оригиналов. `[Рекомендация]`
- Минимизировать лишний JavaScript и не добавлять тяжелую библиотеку анимаций. `[Рекомендация]`

Результат:

- Production-кандидат проходит базовые проверки качества.

Проверка:

- Production build проходит.
- `astro check`, ESLint и Prettier проходят.
- Playwright-проверки проходят для 1440, 1024, 768 и 390 px.
- Проверены отсутствие горизонтального скролла и наложения элементов.

Зависимости:

- Этапы 3-5.

## Этап 7 - Развертывание и CI

Задачи:

- Подготовить Docker-сборку для Node.js runtime. `[Рекомендация]`
- Подготовить nginx-конфигурацию для отдачи приложения или reverse proxy. `[Рекомендация]`
- Настроить GitHub Actions для build/check/lint/format/Playwright. `[Рекомендация]`
- Добавить Astro Node adapter, если форма будет обрабатываться внутри Astro-приложения. `[Рекомендация]` `[Предположение/TBD]`

Результат:

- Проект готов к воспроизводимой сборке и деплою в выбранной инфраструктуре.

Проверка:

- CI выполняет основные проверки.
- Docker-образ собирается.
- Deployment-путь задокументирован.

Зависимости:

- Этап 5: решение по обработке формы.
- Этап 6.

## Этап 8 - Финальная QA и документация

Задачи:

- Проверить все CTA и путь к заявке. `[Рекомендация]`
- Проверить, что в production нет выдуманных данных. `[Рекомендация]`
- Обновить `README.md` командами запуска, проверки и деплоя. `[Рекомендация]`
- Обновить документы, если решения изменились. `[Рекомендация]`

Результат:

- Лендинг готов к клиентскому ревью или деплою.

Проверка:

- Инструкции fresh install/build работают.
- Есть QA-заметки или скриншоты по ключевым viewport.
- Все оставшиеся `TBD` явно задокументированы.

Зависимости:

- Этапы 0-7.

## Логичная последовательность коммитов

1. `docs: fix technical stack`
2. `chore: scaffold astro project`
3. `chore: configure typescript tailwind and quality tools`
4. `feat: add design tokens and base layout`
5. `feat: build header hero and lead form UI`
6. `feat: add landing content sections`
7. `feat: implement astro action form validation`
8. `chore: prepare images and lucide icon system`
9. `test: add playwright responsive checks`
10. `chore: add docker nginx and github actions`
11. `docs: document setup verification and remaining TBDs`

## Ограничения на текущем этапе

- Не начинать реализацию без отдельной задачи. `[Бриф]`
- Не скачивать, не устанавливать и не добавлять зависимости на этапе документации. `[Бриф]`
- Следовать ограничениям из [TECH-STACK.md](TECH-STACK.md). `[Рекомендация]`
- На этапе реализации держать фактический контент в data/config-слое, чтобы неподтвержденные значения было легко проверить. `[Рекомендация]`
